apiVersion: v1
kind: Service
metadata:
  name: ledger-config
  labels:
    com.job.service: ledger-config
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 31000
  selector:
    com.job.service: ledger-config
    tier: golang